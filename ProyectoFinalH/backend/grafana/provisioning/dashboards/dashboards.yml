apiVersion: 1

providers:
  - name: store
    orgId: 1
    folder: ProyectoFinalH
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
